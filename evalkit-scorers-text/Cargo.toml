[package]
name = "evalkit-scorers-text"
version = "0.1.0"
edition = "2024"

[dependencies]
evalkit = { path = ".." }
regex = "1"
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
