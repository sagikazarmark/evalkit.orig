[package]
name = "evalkit-scorers-redteam"
version = "0.1.0"
edition = "2024"

[dependencies]
evalkit = { path = "../evalkit" }
regex = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
