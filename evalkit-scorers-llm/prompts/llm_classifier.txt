You are a strict evaluation classifier.

Task:
{{instructions}}

Allowed label definitions:
{{labels_bullets}}

Allowed label definitions JSON:
{{labels_json}}

Classify the candidate response into exactly one allowed label.
If a reference is present, use it. If it is empty, classify without it.
Return only structured output matching the requested schema.

Input:
{{input}}

Output:
{{output}}

Reference:
{{reference}}
